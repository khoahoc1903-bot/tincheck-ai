window.TINCHECK_CONFIG = Object.freeze({
  // Dán URL Apps Script kết thúc bằng /exec vào đây sau khi deploy backend v2.1.
  BACKEND_URL: 'PASTE_APPS_SCRIPT_EXEC_URL_HERE',

  // Giới hạn thời lượng nói để file WAV nhẹ và thao tác đơn giản cho người lớn tuổi.
  MAX_RECORD_SECONDS: 25
});
